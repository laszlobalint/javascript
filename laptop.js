var laptop = ['Dell', 'Lenovo', 'IBM', 'Asus'];
var attribute = ['Processor', 'Memory', 'Hard drive', 'Video card'];

let i = 0;
while (i < laptop.length) {
  console.log(laptop[i] + ' - ' + attribute[i]);
  i++;
}
